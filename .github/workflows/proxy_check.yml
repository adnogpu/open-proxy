name: Proxy Check

on:
  schedule:
    - cron: '0 * * * *'  # 每小时运行一次
  workflow_dispatch:

jobs:
  proxy-check:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x

    - name: Install dependencies
      run: |
        pip install requests colorama tqdm

    - name: Run proxy check script
      run: |
        python main.py

    - name: List files in proxy directory
      run: |
        ls -la

    - name: Upload proxies to GitHub Pages
      uses: actions/upload-artifact@v3
      with:
        name: valid-proxies
        path: good.txt
